"""Domain logic: recordings, transcription, and the knowledge base.

Pure feature logic over stores/ + core/. The agent pool, schedulers, and dashboard
runner live one layer up in orchestration/ (they run agents; domain does not).
"""
